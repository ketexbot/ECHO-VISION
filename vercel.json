{
  "buildCommand": "ROLLUP_USE_NODE_JS_FALLBACK=1 npm run build",
  "outputDirectory": "dist/spa",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...path]" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
